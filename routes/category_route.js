// const express = require('express');
// const router = express.Router();
// const { protect } = require('../middleware/auth_middleware');
// const {
//   getCategories,
//   createCategory,
//   updateCategory,
//   deleteCategory,
// } = require('../controllers/category_controller');

// // Route: GET /api/categories
// // Access: Private
// router.get('/product', protect, getCategories);  // Make sure getCategories is correctly imported

// // Route: POST /api/categories
// // Access: Private
// router.post('/product', protect, createCategory);  // Make sure createCategory is correctly imported

// // Route: PUT /api/categories/:id
// // Access: Private
// router.put('/:id', protect, updateCategory);  // Make sure updateCategory is correctly imported

// // Route: DELETE /api/categories/:id
// // Access: Private
// router.delete('/:id', protect, deleteCategory);  // Make sure deleteCategory is correctly imported

// module.exports = router;
